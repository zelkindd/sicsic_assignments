Requirements Review - sicsic_c_1
=================================

Section 0: PASS
- 1-2 digit non-negative input validation works
- 5 cumulative errors -> terminates
- Valid input resets error counter

Sections 1-5: PASS
- All implemented correctly

Sections 6, 7, 8: CONFLICT IN THE ASSIGNMENT ITSELF
- PDF explicitly requires: dynamically allocate a temp Stock** array, sort through it,
  print from it, then free() it. Do NOT modify the original array.
- Current code sorts the original stocks[] array directly.
- HOWEVER: Note 7 in the PDF says "in any discrepancy between instructions and the
  run file, follow the run file."
- The expected output.txt shows sorted order persisting when option 2 (print) is called
  after a sort — only possible if the original array was modified.
- CONCLUSION: Current implementation matches the run file (PASS), but violates the
  written instructions. This is the assignment's conflict, not ours.

Section 9: PASS - Palindrome check works correctly.

Section 10: PASS - Exit + "Exiting program..." + memory cleanup correct.

Non-code Requirements:
-----------------------
[PASS] Note 4  - Only uses stdio.h, string.h, stdlib.h
[PASS] Note 10 - All required functions implemented with exact signatures
[PASS] Note 8  - Equal sort values keep original order (bubble sort is stable)
[FAIL] Note 11 - Both student IDs must be written in a comment inside the file
[FAIL] Note 11 - File must be named ID1_ID2.c (currently named main.c)

Action items before submission:
---------------------------------
1. Add both student IDs as a comment at the top of the file
2. Rename the file to ID1_ID2.c (e.g. 123456789_987654321.c)
